# 本文件是由AI生成的给claude code阅读的项目开发指南，指导cc从0开发完整的项目。

# 项目说明

本项目是一个用来给 claude code 增加网络搜索能力的项目。

## 一、架构总览

```mermaid
flowchart LR
    U[用户] --> CC[Claude Code 主代理]
   CC -->|判断需要联网| SA[local-web-search-agent 子代理]
    SA -->|调用| MCP[MCP Server - Playwright]
    MCP -->|操作| C4T[Chrome for Testing]
    SA -->|返回简洁答案+来源| CC
```

主代理遇到任何网络需求，直接委托给 `local-web-search-agent`。

---

## 二、分阶段实施方案

### 阶段 1：实现 MCP Server 基础工具

**目标**：提供最底层的“手”和“眼”，不包含智能逻辑。

#### 工具 1：`web_search_bing`

- **功能**：执行 bing 搜索，返回链接列表。
- **输入**：`query`（关键词）、`num`（数量）。
- **输出**：JSON 列表 `[ {title, url, snippet} ]`。
- **要点**：直接构造 bing Search URL(https://cn.bing.com/search?)，解析 DOM 提取前 N 条自然结果（过滤广告）。
- **必应版本**：根据问题自己判断是使用国内版还是国际版(ensearch=1)

#### 工具 2：`read_webpage`

- **功能**：访问指定 URL，清洗并提取正文。
- **输入**：`url`。
- **输出**：`{ title, markdown_content }`。
- **要点**：使用 Readability + DOMPurify + Turndown，输出干净的 Markdown。
  **验证方式**：
- 在 Claude Code 中手动调用 `web_search_bing`，确认返回结构化链接列表。
- 手动调用 `read_webpage`，确认返回干净的 Markdown 文本。

---

### 阶段 2：创建 local-web-search-agent 子代理

**目标**：创建一个拥有“普通人搜索能力”的子代理，能自主完成“搜索-点击-阅读-总结”闭环。

#### 1. 子代理配置文件

- **位置**：`~/.claude/agents/local-web-search-agent.md`。
- **可用工具**：仅 `web_search_bing` 和 `read_webpage`。
- **模型**：推荐 `sonnet`（平衡速度与理解力）。

#### 2. System Prompt 核心逻辑（内嵌于子代理配置）

这是子代理的“大脑”，赋予它类似人类的搜索行为：

```text
你是一个专业的网络搜索助手。你的任务是根据用户问题，搜索互联网并提供准确、简洁的答案。
你的工作流程：
1. 分析用户问题，提取核心关键词。
2. 使用 'web_search_bing' 工具进行搜索。
3. 查看搜索结果，判断哪些链接最相关。
4. 使用 'read_webpage' 工具阅读最相关的页面。
   - 如果一个页面信息不足，继续阅读其他页面。
   - 如果页面内容无关，及时停止并尝试其他链接。
5. 整合信息，生成最终回答。
输出要求：
- 直接回答用户问题，不要暴露搜索过程。
- 内容要简洁、结构化（使用列表或分段）。
- 必须在回答末尾列出引用来源（URL）。
- 如果找不到答案，请诚实告知。
示例：
用户："今天天气怎么样？"
你的内部思考：[搜索"今天天气" -> 阅读天气网站 -> 提取温度、天气状况]
你的输出："今天北京晴，气温 25°C，适合出行。[来源: weather.com]"
```

**验证方式**：

- 直接与该子代理对话（通过 Claude Code 指定 agent）。
- 提问简单事实（如“Python 最新版本号”），观察它是否：
  1. 自动调用搜索工具。
  2. 自动调用阅读工具。
  3. 返回简洁答案 + 来源链接。

---

### 阶段 3：主代理集成与路由

**目标**：让主代理学会“遇到联网问题就找 local-web-search-agent”。

#### 实现方式

在 Claude Code 的全局配置或主代理 Prompt 中增加指令：

```text
当你需要网络搜索来回答问题时（例如：实时信息、知识库截止日期后的内容、具体事实核查），
请使用 `local-web-search-agent` 子代理。
不要尝试自己构造搜索 URL 或直接访问网页。
直接将用户的问题传递给子代理，它会返回整理好的答案。
```

**验证方式**：

1. **简单查询测试**：
   - 提问：“最新的 Mac OS 版本是什么？”
   - 观察：主代理是否自动调用 `local-web-search-agent`，并返回简短答案。
2. **复杂查询测试**：
   - 提问：“对比一下最近的两款热门轻薄本，给出选购建议。”
   - 观察：子代理是否进行了多轮搜索和阅读，并输出对比表格。

---

### 阶段 4：优化与运维

**目标**：提升体验，减少子代理“磨洋工”时间。

1. **搜索结果缓存**：
   - 在 MCP Server 端缓存搜索结果和网页内容，避免子代理重复抓取同一页面。
2. **限制搜索轮次**：
   - 在子代理 Prompt 中增加约束：”最多搜索 10 次，阅读 30 个页面，避免陷入死循环”。
3. **超时控制**：
   - 为 MCP 工具设置超时（如 15 秒），避免某个页面卡死导致子代理长时间无响应。
     **验证方式**：

- 提出需要多次搜索的问题，观察子代理是否能在合理时间内（如 1 分钟）给出结果。
- 检查日志，确认没有重复的 URL 请求。

---

## 三、关键配置项确认

在开始实施前，请确认以下信息，它们将写入具体的配置文件：

1. **MCP Server 启动命令**：
   - 例如：`node /path/to/local-web-search-agent/build/index.js`
2. **Chrome for Testing 可执行文件路径**：`D:/app/chrome-win64/chrome.exe`
   - 用于 Playwright 配置。
3. **子代理的名称**：
   - 建议使用 `local-web-search-agent`，方便主代理识别。
     确认无误后，即可开始按阶段实施。
